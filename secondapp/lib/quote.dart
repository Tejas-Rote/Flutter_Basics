class Quote {
  String text; // add? to make not nullable variable
  String author;

  Quote({required this.text, required this.author});
// said to add required parameter as these can be null but they are not nullable
}